\\Check if the parameter vibe is true, and if so, return true. Otherwise return false. 
flossin boolin exampleFunction:boolin vibe: { 
  vibeCheck :vibe=based: {
    dab based;;
  } badVibes {
    dab unbased;;
  }
}
